# Вопросы для коллеги на RunPod Claude Code

**Цель:** понять реальную рабочую структуру на RunPod до перепроектирования этапов 7–15 Experiment 003.  
**Контекст:** предыдущий план содержал непроверенное допущение о зеркальной структуре каталогов VPS ↔ RunPod.  
**Время ответа:** 10–15 минут.

---

## 1. Рабочий каталог и структура

1. Какой полный путь к рабочему каталогу проекта на RunPod? (`/workspace/hra-finetuning`, `/workspace/hr-assistant`, другой?)
2. Приведи полную структуру каталогов этого рабочего каталога (например, `tree -L 3` или `find . -maxdepth 3 -type d`).
3. Есть ли на RunPod отдельный git-репозиторий? Если да — в каком каталоге и какой remote/branch?
4. Как соотносятся пути на RunPod с путями в репозитории VPS? Это зеркальная копия или собственная структура?
5. Есть ли симлинки, mount points или переименованные каталоги?

## 2. Данные и модели

6. Где физически хранятся `data/train.jsonl`, `data/validation.jsonl`, `data/test.jsonl`, `data/holdout.jsonl`? Внутри рабочего каталога или на отдельном volume?
7. Есть ли уже скопированные dataset Experiment 002 / Experiment 003? Если да — по каким путям?
8. Где хранятся скачанные модели HuggingFace? (`/workspace/.cache/huggingface`, `~/.cache/huggingface`, другой?)
9. Где сейчас находится best adapter Experiment 002 (`runs/experiment_002/best_adapter`)?
10. Persistent volume: что сохраняется между перезапусками Pod, а что теряется?

## 3. Python environment и зависимости

11. Какой Python используется? (`python`, `python3`, версия?)
12. Где установлены зависимости? (system Python, virtualenv, conda, pipenv, uv, другой?)
13. Есть ли файл `requirements.txt` на RunPod? В каком каталоге и совпадает ли он с VPS?
14. Можно ли установить новые зависимости самостоятельно или это требует действий пользователя?
15. Проверь, пожалуйста, что следующие импорты работают: `torch`, `transformers`, `peft`, `trl`, `datasets`, `psycopg2`, `fastapi`, `uvicorn`.

## 4. Git workflow

16. Как часто / в какой момент RunPod подтягивает изменения из репозитория VPS? (`git pull`, ручной, автоматический при старте?)
17. Может ли RunPod Claude Code делать `git commit` и `git push`? Если нет — как он передаёт отчёты и логи?
18. Какой git-конфиг (user.name, user.email) настроен на RunPod?
19. Будут ли конфликты, если VPS и RunPod одновременно изменяют разные файлы? Как разрешаются?
20. Есть ли ограничения по сети / SSH / доступу к git remote на RunPod?

## 5. Запуск обучения

21. Запускался ли уже `train_lora.py` на RunPod? Если да — приведи точную команду, которая работала.
22. Какой путь к dataset использовался при последнем успешном запуске?
23. Куда сохранялись чекпоинты и best adapter при последнем запуске?
24. Как долго длилось обучение Experiment 002? Какой GPU и сколько VRAM?
25. Есть ли ограничения по времени жизни Pod или сессии?

## 6. Evaluation и runtime API

26. Запускался ли `evaluate_generation_test.py` на RunPod? Какая команда работала?
27. Запускался ли `hra_qwen_api_lora.py` на RunPod? По какому адресу и порту?
28. Есть ли доступ к RunPod извне (public URL / proxy)? Как он формируется?
29. Можно ли запустить HTTP-сервер на RunPod и обратиться к нему локально (с самого RunPod)?
30. Какой workflow или инструмент использовался для runtime smoke test в Experiment 002? (n8n, curl, Python-скрипт?)

## 7. Безопасность и секреты

31. Где на RunPod хранятся credentials: OpenAI API key, PostgreSQL connection string, HuggingFace token?
32. Используются ли env-переменные, `.env` файл или hardcoded значения?
33. Есть ли доступ к PostgreSQL с RunPod? (IP/порт пользователя, firewall?)
34. Нужно ли передавать secrets через пользователя или они уже настроены?

## 8. Интеграция с n8n и Telegram

35. Есть ли уже настроенная связь RunPod ↔ n8n для Multi Provider Test workflow?
36. Какой URL RunPod использовался в n8n workflow (`khu0q820y5ssqu-8000.proxy.runpod.net` или другой)?
37. Меняется ли этот URL при каждом запуске Pod?
38. Нужно ли обновлять n8n workflow после перезапуска Pod?

## 9. Ограничения и риски

39. Какие ограничения по диску / volume на RunPod?
40. Какие ограничения по пропускной способности сети?
41. Можно ли оставлять Pod включённым на несколько часов / суток без участия пользователя?
42. Что происходит с незавершённым обучением, если Pod неожиданно остановится?
43. Есть ли snapshot / backup persistent volume?

## 10. Предпочтительный порядок взаимодействия

44. Как ты предпочитаешь получать задания от VPS Claude Code? (Markdown-файл в репозитории, краткая инструкция, строгий список команд?)
45. Какие артефакты ты готов фиксировать и возвращать в git / persistent volume?
46. Какие действия ты считаешь потенциально опасными и требующими подтверждения пользователя? (перезапись `runs/experiment_002`, удаление volume, запуск обучения, остановка Pod?)
47. Какой формат отчёта о проделанной работе тебе удобен? (Markdown-файл, JSON, stdout log?)
48. Какие шаги Experiment 003 ты можешь выполнить полностью самостоятельно, а какие требуют участия пользователя?
